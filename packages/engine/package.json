{
  "name": "@orbi/engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "g++ -std=c++20 -o build/orbi-engine src/main.cpp",
    "prebuild": "node -e \"require('fs').mkdirSync('build',{recursive:true})\"",
    "test": "vitest run",
    "dev": "pnpm build"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
