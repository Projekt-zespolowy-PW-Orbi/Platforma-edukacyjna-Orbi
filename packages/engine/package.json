{
  "name": "@orbi/engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cmake --build build -j",
    "prebuild": "cmake -S . -B build -G Ninja -DCMAKE_MAKE_PROGRAM=%cd%/tools/ninja.exe",
    "test": "vitest run",
    "dev": "pnpm build"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
