{
  "name": "@orbi/engine",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cmake --build build -j",
    "prebuild": "cmake -S . -B build -G Ninja",
    "test": "vitest run",
    "dev": "pnpm build"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
